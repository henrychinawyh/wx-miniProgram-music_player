import React, { useState } from "react";
import { View } from "@tarojs/components";

import "./index.scss";

const searchMore = (props) => {
  return <View className="searchMore">searchMore</View>;
};

export default searchMore;
