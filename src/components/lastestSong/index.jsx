import React, { useState } from "react";
import { View } from "@tarojs/components";

import "./index.scss";

const lastestSong = (props) => {
  return <View className="lastestSong">最新单曲</View>;
};

export default lastestSong;
