import React, { useState } from "react";
import { View } from "@tarojs/components";

import "./index.scss";

const newAlbum = (props) => {
  return <View className="newAlbum">newAlbum</View>;
};

export default newAlbum;
